# 核心架构建议（实施版）

## 1. 技术栈选型

### 前端
- **Vue 3 + TypeScript + Vite**：提升组件化能力与开发效率。
- **Element Plus（首选）/ Naive UI（可选）**：快速构建表单、表格、分页、抽屉/弹窗等科研平台常见交互。
- **ECharts + Plotly.js（组合使用）**：
  - ECharts：适合柱状图、折线图、热图、箱线/小提琴图等常规统计图。
  - Plotly.js：强烈建议用于火山图、散点图与高交互场景（缩放、框选、悬浮提示）。
- **字体建议**：Inter、Roboto、PingFang SC（无衬线，提升大量基因名与统计值可读性）。

### 后端
- **Python + FastAPI**：
  - 提供高性能异步 API。
  - 更易整合 NumPy/Pandas 处理大规模表达矩阵。
  - 可通过 `rpy2` 或子进程方式调用 R 脚本。

### 计算引擎
- **R + Bioconductor**：
  - DESeq2：差异表达分析。
  - survival/survminer：生存分析、Cox、KM 曲线。
  - GSVA：通路活性评分。
  - CIBERSORT（或兼容实现）：免疫浸润估计。
  - CNV 分析相关包：突变与拷贝数展示。

### 存储与任务系统
- **PostgreSQL**：用户、任务、结果元数据。
- **对象存储（MinIO/S3）**：图像结果、表格附件。
- **Redis + Celery/RQ**：异步任务与状态管理（排队、重试、超时控制）。

---

## 2. 分层架构

```text
[ Vue3 + Element Plus + ECharts/Plotly ]
                 |
             REST / WebSocket
                 |
            [ FastAPI 网关层 ]
         /         |          \
   [任务编排]   [业务服务]   [鉴权审计]
         \         |          /
     [ R 分析执行层 + Python 数据处理层 ]
                 |
       [ PostgreSQL + Redis + MinIO ]
```

### 说明
1. 前端只负责参数输入、任务提交、结果展示，不直接耦合算法细节。
2. FastAPI 负责参数校验、任务创建、结果聚合。
3. 计算层隔离 R 环境，避免污染 Web 进程；建议使用容器化执行。
4. 大计算全部异步化，前端通过轮询或 WebSocket 获取任务进度。

---

## 3. 前端视觉与交互规范（新增）

## 3.1 整体视觉风格：科学性与公信力

### 色调方案（Scientific Palette）
- **主色**：深邃蓝 `#2C3E50` 或医疗青 `#007A85`（稳定、专业）。
- **辅助高亮**：
  - 上调基因：暗红 `#C0392B`
  - 下调基因：深绿 `#27AE60`
- **背景**：浅灰 `#F4F7F6` 或纯白，保证散点图、热图、密集标签可读。

### 字体与排版
- 全站采用无衬线字体（Inter / Roboto / PingFang SC）。
- 数值列使用等宽数字（tabular-nums）提高统计表可比性。

## 3.2 三段式“实验室”布局

为实现“筛选数据 -> 运行算法 -> 查看结果”的闭环，建议采用三段式结构：

1. **左侧参数配置栏（Collapsible Sidebar）**
   - 癌症类型（TCGA 33 种）
   - 样本分组（高/低风险）
   - 阈值参数（`logFC`、`P-value`）
   - 采用可折叠侧栏/抽屉，为中间画布留出最大空间。

2. **中间核心画布区（Analysis Canvas）**
   - Card 流式布局；每个图（火山图、KM 曲线、热图）是独立卡片。
   - 每张卡片右上角提供“全屏看图”和“下载图像（SVG/PNG/PDF）”。

3. **右侧/底部详情面板（Details Pane）**
   - 点击图中基因或样本后，展示该实体的注释信息、PPI 网络或临床关联。
   - 在宽屏（2K/4K）默认右侧展开，窄屏自动切到底部抽屉。

## 3.3 关键模块交互

### 差异分析模块（Interactive Plots）
- 火山图支持：
  - Hover 显示基因名与统计值。
  - Click 锁定目标基因。
  - **Lasso/Box Select**：框选后联动生成候选基因表。

### 生存分析模块（KM-Curve）
- 左侧参数栏可实时切换分型策略（如按基因表达中位数分组）。
- 曲线图无刷新闪烁更新，保障连续分析体验。

### 免疫浸润与药物敏感性
- 推荐使用分组柱状图、小提琴图。
- 增加显著性标记（`*`, `**`, `***`）以贴合医学论文阅读习惯。

## 3.4 质感增强细节

- **加载状态**：使用 Skeleton Screen 或“生信主题”进度条（替代纯转圈）。
- **暗黑模式**：
  - 背景建议 `#1A1A1B`
  - 图表颜色提高饱和度，保证对比度。
- **响应式**：
  - 主场景面向 PC。
  - 对 2K/4K 大屏支持多卡片并排展示，便于对照分析。

---

## 4. 功能模块设计

## 4.1 差异表达分析模块

### 输入
- 癌症类型（必填）
- 可选分组参数（如高低风险阈值、临床分层条件）

### 输出
1. **DE 火山图**：`log2FC` vs `-log10(padj)`。
2. **Cox 风险图**：HR 与显著性分布。
3. **生存曲线图与校准曲线**：用于预后模型评估。
4. **GSVA 评分图**：展示基因集活性差异。

### 交互
- 图像卡片列表 + 点击放大（Dialog/Drawer）。
- 支持下载 PNG/SVG 与对应数据 CSV。

---

## 4.2 免疫浸润分析模块

### 输入
- 癌症类型（必填）

### 处理
- 基于 CIBERSORT 估计 B 细胞、巨噬细胞、NK 细胞、T 细胞等浸润比例。
- 按风险组/亚组对比免疫细胞分布。

### 输出与交互
- 箱线图/小提琴图/热图。
- 图像支持放大、下载。
- 页面提供“分析说明”区域，解释指标生物学意义。

---

## 4.3 基因突变分析模块

### 输入
- 癌症类型（必填）

### 处理
- CNV 结果整理与关键基因筛选。
- 生成棒棒糖图或频次分布图。

### 输出与交互
- 图像展示 + 放大。
- 背景知识说明：CNV 增益/缺失的潜在临床意义。

---

## 4.4 药物敏感性分析模块

### 输入
- 癌症类型（必填）
- 药物名称（支持模糊查询）

### 输出
- 分页表格字段建议：
  - `sample_id`
  - `drug_name`
  - `sensitivity_score`
  - `cancer_type`
  - `data_source`

### 交互
- 后端分页（`page`, `page_size`）。
- 防抖搜索与加载状态提示（Skeleton/Loading）。
- 结果导出（CSV/Excel）。

---

## 5. API 设计建议（FastAPI）

- `POST /api/v1/tasks/de`：提交差异表达任务
- `POST /api/v1/tasks/immune`：提交免疫浸润任务
- `POST /api/v1/tasks/cnv`：提交突变分析任务
- `GET /api/v1/drug-sensitivity`：查询药物敏感性（支持分页/模糊）
- `GET /api/v1/tasks/{task_id}`：任务状态与进度
- `GET /api/v1/tasks/{task_id}/results`：任务结果列表

响应建议统一结构：

```json
{
  "code": 0,
  "message": "ok",
  "data": {}
}
```

---

## 6. 数据与性能设计

1. **任务异步化**：分析任务入队，避免 API 超时。
2. **缓存策略**：同参数任务命中缓存（可复用图像与结果表）。
3. **结果版本化**：记录算法版本、数据库版本、参数快照，保证可追溯。
4. **大表分页**：药物敏感性必须服务端分页，避免一次加载全量。
5. **静态资源 CDN**：图像文件走对象存储 + CDN，减轻主服务压力。

---

## 7. 安全与合规

- JWT/OAuth2 鉴权。
- 关键操作审计日志（谁在何时跑了什么参数）。
- 输入参数白名单校验，防注入。
- 生信分析环境与 API 进程隔离运行（容器 + 最小权限）。

---

## 8. 里程碑（建议）

1. **MVP（4~6 周）**
   - 完成四大模块页面骨架
   - 跑通 DE + 药敏查询主链路
2. **Beta（6~10 周）**
   - 完成免疫浸润与 CNV 全流程
   - 增加任务队列与缓存复用
3. **Release（10~12 周）**
   - 完成权限、审计、监控与性能压测
   - 形成可部署文档与运维手册

---

## 9. 最小可用目录结构（示例）

```text
project/
  frontend/                # Vue3 + Element Plus
  backend/                 # FastAPI
    app/
      api/
      services/
      schemas/
      workers/
  r-engine/                # R scripts / Bioconductor pipelines
  infra/
    docker/
    k8s/
  docs/
```

该结构支持后续把算法、接口、前端分别独立扩展，适合组学平台长期演进。
