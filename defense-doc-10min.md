# DODA 答辩文档（10 分钟内版本）

## 1. 文档目的与适用范围

本文件用于项目答辩场景，目标是：

1. 用尽量短时间讲清楚系统全貌。
2. 重点突出后端工程设计与实现细节。
3. 提供可直接照读的 PPT 逐页讲稿（每页 30-60 秒，总时长不超过 10 分钟）。

本文基于当前仓库实际代码实现，不以规划文档为准。

## 2. 项目定位与一句话概述

DODA（Disease Omics Data Analysis）是一个面向肿瘤多组学分析的 Web 平台，核心能力是：

1. 用户上传表达矩阵（可选上传分组文件）。
2. 后端异步执行 5 类 R 生信分析（DEG、ESTIMATE、GSVA、Immune Checkpoint、CIBERSORT）。
3. 前端轮询任务状态并展示图表与 CSV 结果。
4. 同时提供药物敏感性分页检索接口。

## 3. 实际架构与技术栈（以代码为准）

> 说明：`architecture.md` 中有 FastAPI 规划描述，但当前落地实现是 Spring Boot。

### 3.1 前端

1. Vue 2 + Vue Router + Element UI。
2. `axios` 调用后端 REST API。
3. Analysis 页面负责文件上传、任务提交、状态轮询、结果展示。
4. Drug 页面负责药敏分页检索、防抖搜索。

### 3.2 中间层（应用编排层）

1. Spring Boot Controller：参数接收与响应。
2. Service：任务编排、异步执行、结果聚合。
3. MyBatis Mapper：SQL 查询与分页。

### 3.3 后端执行层

1. Java 调用 Rscript（支持 direct / conda / WSL 模式）。
2. R 脚本完成统计与绘图，产出 PNG/CSV。
3. 文件系统作为分析任务工作目录（按 taskId 隔离）。

### 3.4 数据层

1. MySQL：`drug_predictions` 表。
2. SQL 初始化脚本：`schema.sql` + `data.sql`。

## 4. 后端核心文件职责总览（答辩重点）

### 4.1 入口与全局配置

1. `backend/doda/src/main/java/com/example/doda/DodaApplication.java`
2. 启动 Spring Boot。
3. 注册全局 CORS（当前为 `allowedOrigins("*")`）。

### 4.2 分析任务接口

1. `backend/doda/src/main/java/com/example/doda/analysis/AnalysisTaskController.java`
2. `POST /api/v1/analysis/run`：接收 `multipart/form-data`（matrixCsv/groupCsv + 参数）。
3. `GET /api/v1/analysis/tasks/{taskId}`：查询状态与结果文件信息。
4. `GET /api/v1/analysis/tasks/{taskId}/files/{analysisTypeId}/{filename}`：下载/预览结果文件。

### 4.3 分析任务编排核心

1. `backend/doda/src/main/java/com/example/doda/analysis/ExpressionAnalysisService.java`
2. 生成 taskId、落盘输入文件、自动分组（可选）、提交线程池异步执行。
3. 串行执行五类分析，按类型汇总成功/失败信息。
4. 检测结果目录中的图片和 CSV，封装到任务结果。
5. 文件读取时做路径归一化与目录边界校验（避免路径穿越）。

### 4.4 R 脚本执行器

1. `backend/doda/src/main/java/com/example/doda/analysis/RExecutor.java`
2. 支持 `auto/direct/conda/wsl` 模式。
3. 支持执行超时控制，超时后强制终止并写入日志。
4. `auto` 模式下优先尝试 conda 运行，失败回退到 direct。

### 4.5 分组生成与任务注册

1. `GroupCsvGenerator.java`：按样本均值 + 中位数/上四分位阈值自动分组。
2. `TaskRegistry.java`：基于 `ConcurrentHashMap` 管理任务状态（内存态）。

### 4.6 药敏业务链路

1. Controller：`DrugPredictionController.java`（参数防护 + 统一响应）。
2. Service：`DrugPredictionService.java`（关键词清洗、分页偏移计算、DTO 映射）。
3. Mapper：`DrugPredictionMapper.java`（按癌种 + 药名模糊查询 + `LIMIT/OFFSET`）。

## 5. 两条关键业务链路（建议重点讲）

### 5.1 链路 A：表达分析异步任务

1. 前端上传文件并提交 `POST /api/v1/analysis/run`。
2. 后端创建 task，状态 `PENDING`，保存输入文件。
3. 若未提供分组文件，自动生成 `group.csv`。
4. 任务进入线程池，状态切换为 `RUNNING`。
5. 按分析类型执行 R 脚本，产生日志、图片、CSV。
6. 每种分析单独记录成功/失败，不因单点失败导致全部中断。
7. 若至少一个分析成功，则任务状态标记为 `SUCCESS`（并在 message 中说明部分失败细节）。
8. 前端轮询状态，结束后按文件接口加载图像和 DEG CSV。

### 5.2 链路 B：药物敏感性分页检索

1. 前端传 `cancerType + drugName + page + page_size`。
2. 后端限制页码与页大小（页大小上限 100）。
3. Mapper 执行两条 SQL：分页数据 + 总数统计。
4. Service 将内部字段映射为前端需要的 snake_case 结构。
5. 返回统一结构：`{ code, message, data }`。

## 6. 工程亮点（答辩可重点强调）

### 6.1 可扩展的异步任务机制

1. 线程池参数可配置（池大小、队列容量、脚本超时）。
2. 高峰期触发拒绝策略，返回“服务器繁忙”，保护系统稳定。

### 6.2 多运行环境兼容

1. 支持 Windows 直接 Rscript。
2. 支持 Conda 环境隔离。
3. 支持 WSL + Conda 方式运行生信依赖（适配本地研发环境）。

### 6.3 安全与稳健处理

1. 文件下载接口做目录边界校验。
2. 参数做兜底与范围限制（例如分页参数）。
3. 脚本执行记录日志，便于故障定位。

### 6.4 用户体验导向

1. 分析任务采用前后端解耦的“提交 + 轮询 + 拉取结果”模式。
2. 结果缺失时前端提供明确提示，不会整页报错。
3. 药敏搜索支持防抖，减少无效请求。

## 7. 当前不足与风险（主动讲，通常加分）

1. `TaskRegistry` 是内存态，服务重启后任务状态丢失。
2. CORS 当前对所有来源开放，生产环境需要白名单化。
3. 分析接口与药敏接口响应风格不完全统一（前者直接 DTO，后者 `ApiResponse` 包裹）。
4. 前端写死 `http://localhost:8081`，未做环境变量与代理统一。
5. `GroupCsvGenerator` 与前端 DEG CSV 解析都使用简单 split，对复杂 CSV 引号场景鲁棒性一般。
6. 测试覆盖度较低，目前只有 `contextLoads()` 级别的基础测试。

## 8. 可执行的迭代计划（面向后续）

### 8.1 短期（1-2 周）

1. 统一响应模型和错误码体系。
2. 前端 API baseURL 改为环境变量。
3. 增加核心链路集成测试（分析任务提交、轮询、文件拉取、药敏分页）。

### 8.2 中期（2-4 周）

1. 将任务状态持久化到 MySQL/Redis。
2. 引入任务 TTL 与结果文件清理策略。
3. 将 CORS 改为可配置白名单。

### 8.3 长期（1-2 月）

1. 引入消息队列与独立 Worker，提高并发吞吐。
2. 完善权限与审计（用户级任务隔离、下载鉴权）。
3. 增加任务取消、失败重试、进度百分比回传。

## 9. 答辩高频问题与建议回答

### Q1：为什么选择异步任务，不直接同步返回？

R 分析耗时和波动都较大，同步请求容易超时。异步任务可以把 Web 请求和计算解耦，系统在负载高时更稳定，也能给用户持续状态反馈。

### Q2：如何保证文件下载安全？

后端先拼接结果目录，再做路径归一化并校验目标路径必须位于任务目录内，防止通过 `../` 访问任意文件。

### Q3：为什么支持 WSL 模式？

生信 R 包在 Linux 环境兼容性更好。项目通过 WSL 运行 R，可在 Windows 开发机上获得接近 Linux 生产环境的一致性。

### Q4：你认为当前最需要优化的点是什么？

任务状态内存化是首要风险。服务重启后状态丢失，建议优先落地持久化任务表 + Redis 缓存。

### Q5：部分分析失败但任务显示 SUCCESS 合理吗？

当前策略是“至少一项成功即 SUCCESS，并在 message 给出失败明细”，有利于用户拿到可用结果。后续可增加 `PARTIAL_SUCCESS` 状态使语义更清晰。

---

## 10. PPT 逐页讲稿（10 分钟内，单页 30-60 秒）

> 建议总页数：11 页  
> 预计总时长：约 9 分 10 秒  
> 每页话术可直接照读，也可按现场节奏压缩。

### 第 1 页：项目背景与目标（45 秒）

**画面要点**

1. DODA 项目定位：肿瘤多组学分析平台。
2. 痛点：分析流程分散、门槛高、结果回传慢。
3. 目标：上传数据后，一站式完成分析与结果展示。

**讲稿**

大家好，我汇报的是 DODA 平台。这个项目要解决的核心问题是，生信分析流程通常分散在脚本和命令行里，使用门槛高、协作成本大。我们做的事情是把“上传表达矩阵、执行多种分析、查看图和表”整合成一个 Web 流程，让研究人员不需要反复切换工具，也能快速得到可解释的分析结果。

### 第 2 页：系统总体架构（50 秒）

**画面要点**

1. 前端：Vue 页面交互。
2. 中间层：Spring Boot 负责接口和任务编排。
3. 执行层：R 脚本负责算法计算。
4. 数据层：MySQL 存储药敏数据。

**讲稿**

系统采用四层结构。前端负责参数输入和结果展示；中间层是 Spring Boot，承担接口、参数处理和任务调度；执行层是 R 脚本，完成 DEG、GSVA、CIBERSORT 等算法；数据层是 MySQL，主要提供药物敏感性查询。这个拆分让 Web 接口和算法实现解耦，后续替换或扩展算法时，对前端影响很小。

### 第 3 页：技术栈与选型依据（45 秒）

**画面要点**

1. Vue2 + Element UI：快速构建科研场景页面。
2. Spring Boot + MyBatis：开发效率与 SQL 可控性平衡。
3. Rscript + Conda/WSL：兼容生信包生态。

**讲稿**

选型上我们优先考虑稳定和可落地。前端用 Vue2 和 Element UI，快速实现文件上传、表格和分页。后端用 Spring Boot + MyBatis，接口开发快，同时 SQL 可以精确控制分页和筛选。算法层坚持 R 生态，通过 Conda 和 WSL 管理环境，保证复杂生信依赖能跑通。

### 第 4 页：后端目录与职责拆分（50 秒）

**画面要点**

1. Controller：对外 API。
2. Service：任务编排和业务逻辑。
3. Mapper：数据库查询。
4. analysis 包：任务模型、执行器、分组策略。

**讲稿**

后端代码的拆分很清晰。Controller 层只负责接收参数和返回结果；Service 层做核心编排，比如创建任务、提交线程池、汇总分析结果；Mapper 层专注 SQL 查询。analysis 包是项目亮点，里面有任务状态模型、R 执行器、自动分组工具，保证分析链路既可配置又可维护。

### 第 5 页：核心链路一：表达分析异步执行（60 秒）

**画面要点**

1. 提交任务：`/api/v1/analysis/run`。
2. 后端落盘输入，创建 taskId。
3. 线程池异步执行 5 类分析。
4. 前端轮询 `/tasks/{taskId}` 获取状态和结果。

**讲稿**

这是最关键的一条链路。用户上传矩阵后，后端先生成 taskId 并保存输入文件。若没有分组文件，就自动生成 group.csv。随后任务进入线程池异步执行，依次调用 5 个 R 脚本。每个脚本都输出日志和结果文件，后端再统一收集。前端每两秒轮询一次任务状态，完成后再拉取图片和 CSV。这个机制能避免同步超时，也能让用户看到可追踪的执行过程。

### 第 6 页：R 执行引擎与中间件能力（55 秒）

**画面要点**

1. 支持 direct/conda/wsl 三种执行模式。
2. 脚本超时强杀，避免任务无限挂起。
3. 日志落盘，便于问题定位。
4. 路径校验防目录穿越。

**讲稿**

执行器层面我们做了通用化封装。RExecutor 可在 direct、conda 和 wsl 三种模式切换，兼容不同部署环境。每个任务都支持超时控制，超时会强制终止并记录错误日志，防止线程被长时间占用。文件下载接口对路径做归一化和目录边界校验，避免通过路径拼接访问到任务目录外的文件。这些设计保证了稳定性和安全性。

### 第 7 页：核心链路二：药敏分页检索（45 秒）

**画面要点**

1. 接口：`/api/v1/drug-sensitivity`。
2. 条件：癌种必填 + 药名模糊检索。
3. 服务端分页：`LIMIT/OFFSET` + count。
4. 前端防抖输入，减少压力。

**讲稿**

第二条链路是药物敏感性查询。前端传癌种、药名关键字和分页参数，后端先做参数安全限制，再执行分页 SQL 和总数统计。返回结构是统一的 code/message/data，前端直接驱动表格和分页控件。为了减少无效请求，前端药名输入做了 350 毫秒防抖，体验和性能都更平衡。

### 第 8 页：数据库与接口设计（50 秒）

**画面要点**

1. 表结构：`drug_predictions`，联合主键 + 索引。
2. 初始化：`schema.sql` + `data.sql`。
3. 文件型结果：按 taskId 目录隔离。
4. 配置化：端口、数据源、线程池、上传大小均可调。

**讲稿**

数据库方面，`drug_predictions` 用样本和药物作为联合主键，并给癌种和药名建立索引，支撑检索效率。初始化通过 schema 和 data 脚本自动执行。分析结果没有直接入库，而是按 taskId 存文件目录，这样对图像和大 CSV 更友好。系统关键参数都放在配置中，例如数据源、线程池、脚本超时和上传大小，部署灵活性较高。

### 第 9 页：质量保障、稳定性与风险（55 秒）

**画面要点**

1. 已实现：限流、超时、日志、参数兜底。
2. 主要风险：任务状态仅在内存、CORS 过宽。
3. 兼容性风险：简单 CSV split 处理复杂场景不足。

**讲稿**

质量保障上，当前版本已经具备线程池限流、脚本超时、日志追踪和参数兜底机制。风险点也很明确：任务状态存在内存里，服务重启会丢；CORS 目前放开到所有来源，生产环境不够安全；此外 CSV 解析使用简单 split，在复杂引号和嵌套分隔场景下鲁棒性有限。我们在答辩中主动说明这些问题，体现对系统边界的清晰认知。

### 第 10 页：迭代规划与预期收益（45 秒）

**画面要点**

1. 短期：统一响应、环境变量化、补测试。
2. 中期：任务持久化、结果清理、CORS 白名单。
3. 长期：消息队列 Worker、任务取消重试、权限审计。

**讲稿**

后续迭代我们分三步。短期先做接口规范统一、前端 API 环境变量化和集成测试补全。中期把任务状态持久化，并增加结果清理机制和 CORS 白名单。长期目标是引入消息队列和独立 Worker，实现任务取消、失败重试以及更完善的权限审计。这样系统可以从可用走向可运营。

### 第 11 页：总结与答辩收口（50 秒）

**画面要点**

1. 已完成：端到端可运行链路。
2. 价值：降低生信分析使用门槛。
3. 可扩展：架构已具备工程化演进基础。

**讲稿**

总结一下，这个项目已经完成了“前端上传、后端编排、R 执行、结果展示、药敏查询”的端到端闭环。它的核心价值是把复杂的生信分析流程产品化，降低使用门槛并提升复用效率。虽然还有任务持久化和安全收敛等优化项，但整体架构和模块边界已经稳定，具备持续演进到生产级平台的基础。我的汇报完毕，谢谢各位老师。

---

## 11. 现场答辩节奏建议（可选）

1. 如果老师打断提问，优先保留第 5、6、9、11 页。
2. 时间紧张时，第 3 和第 8 页各压缩到 30 秒。
3. 若被追问“为什么不是 FastAPI”，直接回答“当前交付是 Spring 实现，规划文档会同步更新”。



实现模糊查询 ：DrugPredictionMapper 的 like
Mybaits拼接查询名称